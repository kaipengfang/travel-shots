# 开发日志

> 摄影作品展示网站开发记录

---

## 2026-02-17

### 新增
- **CLIP 智能排序**：使用 CLIP 视觉模型对相册照片进行智能排序
  - TSP + 最佳断点算法，最小化相邻照片视觉差异
  - 处理所有 13 个相册，共 252 张照片
  - 平均相邻相似度 0.45-0.65，首尾相似度 0.02-0.13
- **瀑布流贪心预排序**：解决 CSS Columns 垂直填充导致的顺序问题
  - 模拟列高平衡，预先计算照片分配
  - 实现接近水平的视觉顺序
  - 解决 SSR/CSR Hydration 不匹配问题
- **气泡框优化**：多相册限制显示 3 个，添加相册/照片数量统计

### 修复
- 气泡框定位算法：统一尺寸计算与实际 CSS 一致
- 香港相册数据：从原始 JSON 重新导入完整数据（EXIF、caption）

---

## 2026-02-16

### 新增
- **数据层级重构**：Region → Album → Photo 三层结构
- **美国相册拆分**：西雅图、雷尼尔山、华盛顿州
- **省份点亮**：中国省份使用 adcode 匹配

### 优化
- 地图尺寸放大（1400×820）
- 气泡框智能定位算法 v9（8 方向自适应）

---

## 2026-02-15

### 新增
- **灯箱毛玻璃效果**：backdrop-blur 40px + saturate 120%
- **React Portal 渲染**：修复父元素 transform 导致的定位 bug

### 修复
- 灯箱在页面滚动后位置错误
- 图片与底部信息栏重叠（改用 flex 布局）
- PhotoMap 类型错误（countryValue 改为可选）

### 优化
- 导航按钮放大（56px）+ 半透明背景 + SVG 图标
- 图片最大尺寸限制，与信息栏保持间距

---

## 2026-02-14

### 新增
- **点赞评论功能**（Supabase）
  - AlbumInteraction 组件，位于相册底部
  - 支持对整个相册点赞
  - 支持评论（显示最近 20 条）
  - 使用 localStorage 记录用户点赞状态

### 修复
- 气泡框切换标记点时不触发动画（添加 `key={popupId}`）
- 夜间模式背景色不一致（统一为 `#0a0f1a`）
- 单相册地区点击预览图跳转逻辑错误

### 优化
- 气泡框添加淡淡金色发光
- 灯箱详情页按钮交互（悬停显示、放大效果）
- 灯箱关闭按钮移至右上角

---

## 2026-02-13

### 新增
- 项目初始化
- 世界地图（d3-geo）+ 中国省份叠加
- 去过的国家/省份点亮效果
- 地图拖拽、缩放、视图切换
- 标记点 + 气泡提示
- 瀑布流布局
- 灯箱详情页
- 深色模式
- 语言切换（中/英）

---

## 2026-02-18

### 重构：数据层 JSON 化 ✅

**目标：** 用户只需新建/编辑 `src/data/regions/*.json`，网站自动更新，无需改动任何代码。

**完成内容：**

1. **清理废弃文件** — 删除 9 个废弃 ts、3 个临时文件、4 个根目录垃圾 json
2. **拆分 photos.ts → regions/*.json** — 9 个地区，232 张照片，图片路径改为相对路径
3. **新建 `src/data/config.json`** — `photoBaseUrl`、`siteName`、`motto`
4. **新建 `src/data/index.ts`** — 自动扫描 regions/，动态计算所有派生数据
5. **新建 `src/lib/photoUrl.ts`** — 统一拼接图床 URL 的工具函数
6. **更新 4 处 import 路径** — `@/data/photos` → `@/data`，座右铭/网站名读 config.json，图片路径用 photoUrl()
7. **新建 `.env.example`**
8. **更新 docs/README.md** — 删除明文 API Key，更新项目结构和部署流程说明

**验证：** `npm run build` 零错误通过。

---

*创建于 2026-02-13*
